// Authored preview — the landing section. A simple intro heading + tagline
// shown before the game starts.
import { HomePage } from "ideophone-arena-web";

export const Default = () => <HomePage />;
